import { render, screen } from '@testing-library/react'


describe('ExpandableText', () => {
  it('should render the text and a button', () => {
    // Arrange
    // Act
    // Assert
  })

  it('should expand the text when the button is clicked', () => {
    // Arrange
    // Act
    // Assert
  })
})